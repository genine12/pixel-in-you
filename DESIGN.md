# DESIGN.md: 미스터리 신호 복원 (가칭)

## 1. 디자인 무드

**컨셉 한 줄**: 검정 화면 위에서 옅은 빛이 번지듯 새어나오는 낡은 CRT 터미널 — 우주선 콘솔이 미확인 신호(사진)를 한 조각씩 복원해나가는, 살짝 초점이 나간 듯 몽환적인 화면.

레퍼런스에서 추출한 핵심 요소:
- 검정 배경 + 옅은 크림빛 옐로우 모노스페이스 텍스트 — 쨍한 명암 대비보다는 빛이 경계 없이 번져나가는 느낌
- 스캔라인 / 미세한 플리커 / 크로마틱 애버레이션(빨강·청록 색번짐)으로 "오래된 브라운관" 질감을 내되, 전체적으로는 흐릿한 빛무리(bloom)가 감도는 몽환적 톤
- 좌표·수치 데이터가 계속 흘러다니는 HUD 오버레이
- 사진(실제 데이터)과 UI 크롬(옅은 옐로우 형광)의 명확한 분리 — 단, 본 프로젝트에서는 리빌 전까지 사진도 이 톤에 편입됨

---

## 2. 컬러 토큰

| 토큰 | 값(예시) | 용도 |
|---|---|---|
| `--bg-void` | `#050503` | 전체 배경 (완전 검정 아님, 미세하게 밝은 블랙) |
| `--bg-panel` | `#0a0a05` | 패널/카드 배경 |
| `--phosphor-primary` | `#F1FEC6` | 메인 포인트 컬러 (텍스트, 라인, 아이콘) — 옅은 크림빛 옐로우 |
| `--phosphor-dim` | `#7A7E52` | 비활성/보조 텍스트, 그리드 라인 |
| `--phosphor-glow` | `#F1FEC6` (강한 blur 처리, 여러 겹 레이어) | text-shadow / box-shadow 글로우 & 빛번짐(bloom) 전용 |
| `--phosphor-halo` | `rgba(241,254,198,0.25)` | 넓은 반경의 radial-gradient 빛무리 배경 연출 |
| `--terminal-white` | `#FCFEF0` | 강조 텍스트 (거의 흰색이지만 아주 옅은 크림 틴트) |
| `--danger-amber` | `#FFB020` | 에러/색상 불일치/경고 상태 전용 (극히 제한적으로만 사용) |
| `--grid-line` | `rgba(241,254,198,0.12)` | 배경 격자선 |
| `--scanline-overlay` | `rgba(0,0,0,0.12)` | 스캔라인 반복 패턴 |

**원칙**: 포인트 컬러는 옅은 크림빛 옐로우(`#F1FEC6`) 하나로 통일 — 채도를 낮춰 쨍하게 튀지 않고 은은하게 번지는 톤. 앰버(경고색)는 색상 매칭 실패, 세션 소진 같은 "주의가 필요한 상태"에만 예외적으로 사용 — 남발 금지.

---

## 3. 타이포그래피

- **폰트**: 모노스페이스 전용 (예: `JetBrains Mono`, `IBM Plex Mono`, `Space Mono`)
- 모든 데이터/숫자/좌표/타이머는 반드시 모노스페이스로 — 자릿수가 흔들리면 터미널 느낌이 깨짐
- 헤더는 자간(letter-spacing)을 넓게 (`0.15em` 이상), 대문자 사용 권장 (예: `MISSION CONTROL`, `SIGNAL RESTORATION`)
- 본문/설명 텍스트는 가독성을 위해 자간 기본값 유지
- 강조 수치(진행률 %, 좌표값)는 `--phosphor-primary` + 살짝의 text-shadow 글로우

---

## 4. 레이아웃 원칙

- **HUD 프레임 구조**: 모든 주요 섹션은 얇은 1px 옅은 옐로우 보더(빛번짐 처리)로 감싼 사각 패널 (둥근 모서리 없음 — 완전 각짐)
- **그리드 기반**: 대시보드는 콘솔 계기판처럼 여러 패널이 격자로 분할된 레이아웃 (참고: 레퍼런스 5번의 상단바+이미지 프레임+텍스트 박스+진행바 구조)
- **스타맵(그리드 뷰)**: 셀 사이 간격 최소화, 각 셀은 정사각형, 빈 셀은 점선 테두리 + 좌표 라벨만, 채워진 셀은 옅은 옐로우 듀오톤 필터 처리된 썸네일 (은은한 빛번짐 포함)
- **진행바**: 레퍼런스 5번처럼 0~100 눈금 형태의 수평 바 + 현재 위치 삼각형 마커
- **여백**: 콘솔 계기판 느낌을 위해 여백은 넉넉하지 않게 — 정보 밀도가 있어야 "터미널"답다 (단, 텍스트 줄 간격은 확보해 가독성 유지)

---

## 5. 텍스처 & 모션

- **빛번짐(bloom)**: 이 프로젝트의 핵심 무드. 텍스트/라인/셀 테두리 주변에 `--phosphor-glow`를 여러 겹(예: blur 4px + 12px + 32px 레이어 중첩)으로 쌓아 경계가 또렷하지 않고 서서히 번져나가는 느낌을 만든다. 배경에는 `--phosphor-halo`로 넓은 radial-gradient를 깔아 화면 전반에 은은한 빛무리가 감돌게 한다
- **초점 나간 느낌**: 일부 배경 요소(격자선, 원경 텍스트)는 아주 살짝 blur 처리해 "완벽히 선명하지 않은" 아날로그 렌즈감을 준다 — 전경 핵심 정보(진행률, 셀 상태)는 선명하게 유지
- **스캔라인**: 화면 전체에 얇은 반복 가로줄 오버레이 (`repeating-linear-gradient`), opacity를 낮게 유지하고 빛번짐과 겹쳐 스캔라인 자체도 살짝 흐릿하게 — 딱딱한 줄무늬가 아니라 빛 사이로 비치는 결 정도로
- **플리커**: 아주 미세한 밝기 변화 애니메이션 (2~4초 주기, 감지될 듯 말 듯한 수준) — 로딩/대기 상태에서만 사용, 상시 적용 시 눈 피로 유발 주의
- **글로우**: 텍스트/라인에 `text-shadow`/`box-shadow`로 은은한 발광 효과 (블러 반경 크게, 투명도 낮게)
- **크로마틱 애버레이션**: 리빌 애니메이션, 중요 알림 등 "특별한 순간"에만 순간적으로 사용 (상시 적용 금지 — 레퍼런스 7번처럼 과하면 피로도 높음)
- **셀 채우기 성공 모션**: 노이즈 텍스처가 스캔라인처럼 한 번 훑고 지나가며 정착, 그 자리에 잠시 빛이 번졌다 가라앉는 잔광 효과
- **리빌 애니메이션**: 그리드 전체에 은은한 빛이 위→아래로 번지듯 훑고 지나가며 그 뒤로 필터가 원색으로 전환되는 연출 (레퍼런스 10번의 "옅은 옐로우 UI 속 원색 사진" 대비를 활용, 단 전환은 급격하지 않고 빛이 서서히 걷히듯 진행)

---

## 6. 사진(픽셀 콘텐츠) 처리 규칙

- **채우는 중 / 미완성 레벨**: 셀 안에 들어간 유저 사진은 전부 통일된 옅은 옐로우 듀오톤 필터 처리 (`grayscale` + `hue-rotate` + `brightness` 조합, 또는 SVG `feColorMatrix`)
  → 유저는 자기가 무슨 사진을 냈는지는 알지만, 전체 그리드에서는 모두 옅은 옐로우 톤으로 통일되어 정체를 가늠할 수 없음
- **리빌 순간**: 필터가 걷히며 각 사진이 원색으로 전환 — 이 순간이 유일하게 "옅은 옐로우빛이 아닌 색"이 화면 전체를 채우는 지점이므로 연출적으로 가장 힘줘야 할 부분
- **레벨 히스토리/갤러리**: 완성된 레벨은 원색 그대로 보관 (옐로우 필터 다시 씌우지 않음)

---

## 7. Never Do 리스트

- ❌ 포인트 컬러에 옅은 옐로우 외 색상(다채로운 파스텔톤, 여러 색이 섞인 장식용 그라디언트 배경 등) 섞지 않기 — 앰버는 경고 상태 전용. 단, 빛번짐(bloom)을 표현하기 위한 단색(옅은 옐로우) radial-gradient/blur는 이 규칙의 예외이며 오히려 핵심 연출 요소
- ❌ 둥근 모서리(border-radius) 남용 금지 — 터미널/콘솔은 각져야 함
- ❌ 부드러운 드롭섀도우 대신 항상 글로우(glow)로 입체감 표현
- ❌ 데이터/숫자에 비-모노스페이스 폰트 사용 금지
- ❌ 스캔라인·플리커·크로마틱 애버레이션을 상시 강하게 적용해 가독성 해치지 않기 (포인트에만 절제해서 사용)
- ❌ 리빌 전 사진 원색이 미리보기 등으로 새어나오지 않게 (옐로우 필터는 서버 처리 단계에서도 고려)
- ❌ 지나치게 매끈한 "미래형 SF 영화" 느낌으로 흐르지 않기 — 목표는 화려한 스페이스 UI가 아니라 "오래된 아날로그 콘솔"의 질감
- ❌ 여백을 과도하게 넓혀 미니멀 앱처럼 보이게 만들지 않기 — 정보 밀도가 콘셉트의 핵심

---

## 8. Open Items

- [ ] 정확한 폰트 패밀리 최종 선정 (JetBrains Mono / IBM Plex Mono / Space Mono 중 택1 또는 조합)
- [ ] 스캔라인/플리커 강도 수치 (opacity, duration) 프로토타입 후 조정
- [ ] 리빌 애니메이션 총 소요 시간 및 이징(easing) 커브
- [ ] 다크 환경 외 라이트 모드 지원 여부 (현재는 다크 전용 전제)
